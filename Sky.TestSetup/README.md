# Sky.TestSetup — Developer README

Purpose
- Provides test utilities and fixtures used across multiple test projects.

Quick start
```powershell
dotnet test SkyCMS.sln
```

Where to look
- Helpers and fixtures: `Sky.TestSetup/*`.

Notes
- Use these utilities to create consistent test contexts and avoid duplicating setup code.
